#include <iostream>

using namespace std;

int main() {
    //Завдання 1

    /*double R1, R2, R3;
    cout << "Enter the values of resistors R1, R2, and R3: ";
    cin >> R1 >> R2 >> R3;

    double R0 = 1 / (1 / R1 + 1 / R2 + 1 / R3);

    cout << "Equivalent resistance R0: " << R0 << endl;*/


    //Завдання 2
    // 
    //double circumference;
    //const double pi = 3.14;

    //cout << "Enter the circumference of the circle: ";
    //cin >> circumference;

    //double radius = circumference / (2 * pi);
    //double area = pi * radius * radius;

    //cout << "Radius of the circle: " << radius << endl;
    //cout << "Area of the circle: " << area << endl;


    //Завданя 3
    // 
    //double v, t, a;

    //cout << "Enter the velocity (v): ";
    //cin >> v;

    //cout << "Enter the time (t): ";
    //cin >> t;

    //cout << "Enter the acceleration (a): ";
    //cin >> a;

    //double distance = v * t + (a * t * t) / 2;

    //cout << "Distance travelled: " << distance << endl;

    //Завданя 4

   /* int seconds, hours, minutes, remainingSeconds;

    cout << "Enter the number of seconds: ";

    cin >> seconds;

    hours = seconds / 3600;

    remainingSeconds = seconds % 3600;

    minutes = remainingSeconds / 60;

    remainingSeconds = remainingSeconds % 60;

    cout << "Hours: " << hours << endl;

    cout << "Minutes: " << minutes << endl;

    cout << "Seconds: " << remainingSeconds << endl;*/




    //Завданя 5

 /*   double amount;

    cout << "Enter the decimal number: ";

    cin >> amount;

    int hryvnia = int(amount);

    int kopeck = int((amount - hryvnia) * 100);

    cout << hryvnia << " UAH " << kopeck << " kop." << endl;*/


    //Завданя 6
    // 
    //double distanceMeters, timeMinutes, timeSeconds, totalTimeSeconds, speedKmph;

    //cout << "Enter the distance (meters): ";
    //cin >> distanceMeters;

    //cout << "Enter the time (minutes seconds): ";
    //cin >> timeMinutes >> timeSeconds;

    //totalTimeSeconds = timeMinutes * 60 + timeSeconds;

    //speedKmph = distanceMeters / 1000 / (totalTimeSeconds / 3600);

    //cout << "Distance: " << distanceMeters << " m" << endl;
    //cout << "Time: " << timeMinutes << " min " << timeSeconds << " sec = " << totalTimeSeconds << " sec" << endl;
    //cout << "You ran at a speed of " << speedKmph << " km/h." << endl;


    //Завданя 7

   /* int days, weeks, remainingDays;

    cout << "Enter the number of days: ";
    cin >> days;

    weeks = days / 7;
    remainingDays = days % 7;

    cout << "Weeks: " << weeks << " and days: " << remainingDays << endl;*/


}